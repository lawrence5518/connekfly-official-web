# Reparación Vercel — Invitación diferida

Versión reconstruida desde `connekfly_official_web_premium_v3.zip`.

Validación realizada:

- `npm run build` completó correctamente.
- La ruta dinámica `/i/[code]` fue generada.
- `package-lock.json` usa únicamente `https://registry.npmjs.org/`.

## Subida

Reemplaza el contenido del repositorio web con el contenido de esta carpeta y confirma el commit. Vercel desplegará automáticamente.

No subas la carpeta contenedora; sube los archivos que están dentro de `connekfly-official-web`.
